<h1>Index</h1>

<hr>

<%= link_to 'Add', '/contacts/new' %>

<ul>
  <% @contacts.each do |contact| %>
       <li><%= link_to contact.name, {action: 'show',id: contact.id} %></li>
  <%end%>
</ul>

<hr />

<%= link_to 'Home', '/' %>
