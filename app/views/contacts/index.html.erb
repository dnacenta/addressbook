<h1>Contact Index</h1>

<ul>
  <% @contacts.each do |contact| %>
      <li><%= contact.name + " | " + contact.address + " | " + contact.phone + " | " + contact.email %></li>
  <%end%>
</ul>

<%= link_to 'Add', '/contacts/new' %>
<%= link_to 'Home', '/' %>
